namespace ECS
{
    /// <summary>
    /// Marker interface for all component types in the ECS framework.
    /// Implement this interface on all structs that represent data components to ensure type safety
    /// and enable generic constraints for ECS operations.
    /// </summary>
    public interface IComponent
    {
    }
}
